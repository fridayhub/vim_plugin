This is vim plugin utility.

Usage:
    mv vim ~/.vim
    mv vimrc ~/.vimrc
    vim ~/.vimrc
    change "let g:pydiction_location = '/home/yourname/.vim/bundle/pydiction/complete-dict'"
    to your dircetory.
